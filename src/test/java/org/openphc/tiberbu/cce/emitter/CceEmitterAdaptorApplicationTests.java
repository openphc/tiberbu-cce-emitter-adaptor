package org.openphc.tiberbu.cce.emitter;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test — verifies the Spring context loads with the default configuration.
 */
@SpringBootTest
class CceEmitterAdaptorApplicationTests {

    @Test
    void contextLoads() {
        // Fails if any bean definition or configuration binding is broken.
    }
}
